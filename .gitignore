# Node.js
node_modules/
npm-debug.log
.npm/
*.tgz

# dotenv environment variables file
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# Express specific (if applicable)
/views/ # if you have a /views folder that generates compiled views you don't want to commit
/public/ # if Express serves its own static assets from a separate public folder that's built

# Vite / React app in client/ sub-directory
# Ignore node_modules and dist within the client/ directory
client/node_modules/
client/dist/
client/.env # for Vite/client-side .env files
client/.env.* # for Vite/client-side .env files

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Build artifacts (for the root app, if any)
build/ # Common build output directory (less common for pure Express API)
.next/ # If you were ever using Next.js

# IDEs and other tools
.idea/
.vscode/
.vs/
*.swp

# Operating System Files
.DS_Store # macOS specific
Thumbs.db # Windows specific